{
  "name": "scratchasm",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Scratch-style block editor and NASM code editor for x86-64 Linux, with accounts, dark mode and a local assemble/run server",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/smoke.mjs && node test/api.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
